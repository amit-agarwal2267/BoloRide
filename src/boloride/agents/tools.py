"""LiveKit discovers typed tools declared directly on ``BoloRideAgent``.

Transport-neutral helper functions remain under :mod:`boloride.tools`. This
module deliberately contains no second tool-schema registry that could drift.
"""
